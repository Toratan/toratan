<?php if(count($this->route)): ?>
<ol class="breadcrumb">
<?php
    $i = 1;
    # loading routes
    foreach ($this->route as $folder) : $is_active = ($i++==count($this->route));?>
        <li <?php echo $is_active?" class='active'":""?>>
            <?php 
                if(!$is_active)
                    echo "<a link-type='folder' class='table-nav-link' href='/frame/e/directory/{$folder->folder_id}.{$this->request->type}' onclick='window.top.document.title = \"/ $folder->folder_title\";'>";
                echo $folder->folder_title;
                if(!$is_active)
                    echo "</a>";
            ?>
        </li>
<?php endforeach; ?>
</ol>
<?php endif; ?>
<?php
    $dt = new modules\frameModule\models\directoryTree($this->request);
    $dt->{"plot{$this->request->type}"}($this->items, $this->pid, $this->is_owner);
?>