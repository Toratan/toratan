<div id="new-link-ui">
    <ol class='text-danger' style="list-style-image: url('/access/img/bullet.png');padding:0;margin-left:15px;">
    <?php foreach ($this->errors as $error) : ?>
        <li><?php echo $error ?></li>
    <?php endforeach; ?>
    </ol>
    <style>
        .double-input .form-control {
            width: 50%;
            border-right-width: 0px;
        }
        .double-input .form-control:focus {
            border-right-width: 1px;
        }
    </style>
    <form action="<?php echo $this->request->GetURI() ?>" method="POST">
    <?php if(isset($this->request->params["continue"])) : ?>
        <input type="hidden" name="continue" value="<?php echo $this->request->params["continue"]; ?>" />
    <?php endif; ?>
        <div class="input-group double-input">
            <input type="text" class="form-control" name="link_title" maxlength="250" placeholder="Link's title" value="<?php echo isset($this->values['link_title'])?$this->values['link_title']:"" ?>"/>
            <input type="text" class="form-control" name="link_body" maxlength="250" placeholder="Link's body" value="<?php echo isset($this->values['link_body'])?$this->values['link_body']:"" ?>"/>
            <span class="input-group-btn">
                <button class="btn btn-primary" type="submit">Create</button>
            </span>
        </div><!-- /input-group -->
    </form>
    <script>
        $(document).ready(function(){
            setTimeout(function() { $('input[name="link_title"]').focus(); }, 500);
            $("#new-link-ui form" ).submit(function(e) {
<?php if(isset($this->values['link_title'])): ?>
                e.preventDefault();
                $.ajax({
                    type: $(this).attr("method"),
                    url: $(this).attr("action"),
                    data: $(this).serialize()+"&suppress_redirection=1&"+$(this).attr("action").split("?")[1],
                    success: function() {
                        window.apply_change(
                            window.APPLY_NAME_EDITTED,
                            $("input[name='link_title']").val()
                        );
                    }
                });
<?php endif; ?>
            });
        });
    </script>
</div>