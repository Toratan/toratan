<style>
    td{padding: 10px;min-width: 10%;}
    td .last{border-bottom: 1px solid #e6e6e6}
</style>
<hr />
<?php if(\core\db\models\user::IsSignedin()): ?>
<div>
        <a href="/ops/new/folder/?pid=0<?php echo \zinux\kernel\security\security::GetHashString(array("folder", 0,  session_id(),  \core\db\models\user::GetInstance()->user_id))?>">
            Create new folder
        </a>| 
        <a href="/ops/new/link/?pid=0<?php echo \zinux\kernel\security\security::GetHashString(array("link", 0,  session_id(),  \core\db\models\user::GetInstance()->user_id))?>">
            Create new link
        </a>| 
        <a href="/ops/new/note/?pid=0<?php echo \zinux\kernel\security\security::GetHashString(array("note", 0,  session_id(),  \core\db\models\user::GetInstance()->user_id))?>">
            Create new note
        </a>
</div>
<?php endif; ?>
<table>
    <thead>
        <tr>
            <td>Folders</td>
            <td>Notes</td>
            <td class="last">Links</td>
        </tr>
    </thead>
    <tbody>
        <?php while(true): ?>
            <tr>
                <td>
                    <?php if(count($this->folders)): $f = array_shift($this->folders);?>
                        <?php echo $f->folder_title; ?>
                    <?php endif; ?>
                </td>
                <td>
                    <?php if(count($this->notes)): $n = array_shift($this->notes);?>
                        <?php echo $n->note_title; ?>
                    <?php endif; ?>
                </td>
                <td>
                    <?php if(count($this->links)): $l = array_shift($this->links);?>
                        <?php echo $l->link_title; ?>
                    <?php endif; ?>
                </td>
            </tr>
        <?php
        if(!count($this->folders) && !count($this->notes) && !count($this->links))
            break;
        ?>
        <?php endwhile; ?>
    </tbody>
</table>